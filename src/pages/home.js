import React from "react";
import { Button } from "reactstrap";
const Home = (props) => {
  return (
    <div>
      <h1>Home</h1>
      <Button color="primary">primary</Button>
    </div>
  );
};

export default Home;
